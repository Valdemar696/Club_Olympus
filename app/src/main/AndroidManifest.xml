<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.clubolympus">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_baseline_sports_kabaddi_24"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClubOlympus">
        <activity
            android:name=".AddMemberActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <provider
            android:authorities="com.example.clubolympus"
            android:name=".data.OlympusContentProvider"
            android:exported="false" />
    </application>

</manifest>